{
  "name": "nirnai-extension",
  "version": "1.3.0",
  "description": "NirnAI - Clear decisions. Every purchase.",
  "scripts": {
    "build": "node build.mjs",
    "build:dev": "NODE_ENV=development node build.mjs",
    "watch": "node build.mjs --watch",
    "watch:dev": "NODE_ENV=development node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.27.4",
    "typescript": "^5.4.0"
  }
}
